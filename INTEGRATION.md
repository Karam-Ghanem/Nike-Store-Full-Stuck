# دليل تكامل Nike Store

يتكوّن المشروع من واجهة React/Vite في `client/` وخلفية Django REST في `server/`. الواجهات البصرية الأصلية في `client/` بقيت كما هي قدر الإمكان، وأضيفت طبقة اتصال مع Django بالإضافة إلى شاشة مصادقة متناسقة مع أسلوب Nike الحالي.

## التشغيل المحلي

شغّل الخلفية أولاً من مجلد `server/`. يلزم تطبيق migrations قبل التشغيل كي تُنشأ جداول الكوبونات وسياسة الإرجاع وطلبات الإرجاع.

```bash
cd server
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 8000
```

شغّل الواجهة من مجلد `client/` في نافذة طرفية أخرى.

```bash
cd client
npm ci
npm run dev
```

تتصل الواجهة افتراضياً بـ`http://127.0.0.1:8000/api`. يمكن تغيير ذلك من خلال المتغير `VITE_API_BASE_URL`، مثال:

```bash
VITE_API_BASE_URL=https://api.example.com/api npm run dev
```

## تدفق المستخدم

يمكن للمستخدم إنشاء حساب أو تسجيل الدخول من أيقونة المستخدم في الترويسة أو من قائمة الجوال. تحفظ الواجهة Token الخاص بـDjango محلياً، ثم تستعمله تلقائياً مع السلة والمفضلة والطلبات والكوبونات والإرجاع. يظهر زر تسجيل الخروج في المكان نفسه بعد تسجيل الدخول.

تستخدم الواجهة البيانات الحقيقية من Django للمنتجات والسلة والمفضلة. عند تأكيد الدفع، تُنشئ طلباً من بيانات العنوان وعناصر السلة، وتتحقق الخلفية من المخزون وتحسب الخصم وتخصم المخزون ضمن معاملة واحدة. تعرض صفحة **My Purchases** عناصر الطلبات وتسمح بطلب إرجاع العنصر ضمن المدة المعرّفة في سياسة الإرجاع.

## المسارات الجديدة أو الموسعة

| الغرض | المسار | الصلاحية |
| --- | --- | --- |
| التسجيل | `POST /api/auth/register/` | عام |
| تسجيل الدخول | `POST /api/auth/login/` | عام |
| المستخدم الحالي | `GET /api/auth/user/` | مستخدم مسجل |
| تسجيل الخروج | `POST /api/auth/logout/` | مستخدم مسجل |
| إدارة الكوبونات | `/api/coupons/` | مدير |
| التحقق من الكوبون | `POST /api/coupons/validate/` | مستخدم مسجل |
| سياسة الإرجاع | `GET/PATCH /api/return-policy/` | القراءة عامة؛ التعديل مدير |
| طلبات الإرجاع | `/api/returns/` | مستخدم مسجل؛ المدير يرى الكل ويعالج الطلبات |
| اعتماد أو رفض إرجاع | `POST /api/returns/{id}/approve/` و`/reject/` | مدير |
| إنشاء وعرض الطلبات | `/api/orders/` | مستخدم مسجل |
| Reviews العامة | `GET/POST /api/reviews/` | القراءة عامة؛ الإضافة مستخدم مسجل |
| Reviews الإدارة | `DELETE /api/reviews/{id}/` | مالك المراجعة أو مدير |
| مؤشرات dashboard | `GET /api/admin/dashboard/` | مدير |
| إدارة المنتجات | `/api/products/` | القراءة عامة؛ الكتابة مدير |
| أرشفة المنتج | `POST /api/products/{id}/archive/` و`unarchive/` | مدير |
| تخفيض منتج | `POST /api/products/{id}/set_discount/` | مدير |
| تخفيض جماعي | `POST /api/products/bulk_discount/` | مدير |

## Reviews ولوحة التحكم

تجلب صفحة **Reviews** المراجعات النصية من Django وتضيف المراجعة بعد تسجيل الدخول، بينما تعرض صفحة **Users Reviews** في لوحة التحكم المراجعات المحفوظة وتتيح للمدير حذفها. بقيت عناصر الاستبيان الموجودة في الواجهة معروضة للحفاظ على التصميم، وتُضاف المراجعات النصية الجديدة إلى قاعدة البيانات.

تقرأ بطاقات dashboard الأساسية عدد المراجعات والمبيعات والمستخدمين والمنتجات النشطة من `/api/admin/dashboard/`. نموذج **Add/Edit Product** يرسل بيانات المنتج والمقاسات والمخزون والصورة إلى Django بصيغة multipart، وصفحة **Archive** تستخدم عمليتي archive وunarchive، بينما صفحة **Sale** تستخدم التخفيض الفردي أو الجماعي وتحدّث السعر القديم والجديد في قاعدة البيانات.

## قواعد الأعمال المطبقة

ينشئ مدير النظام كوبوناً من شاشة الإدارة الحالية، ويُنشأ الكوبون الجديد بنسبة خصم افتراضية قدرها **50%** لتتوافق مع النص الموجود في واجهة السلة. يجري التحقق من صلاحية الكوبون وتاريخه والحد الأدنى للطلب في Django، وليس في الواجهة فقط. يمكن للمدير تغيير الفترة الموحدة للإرجاع من صفحة **Product return period**، وتُطبّق تلك المدة على الطلبات الجديدة عند إنشائها.

إنشاء طلب الإرجاع لا يعيد المخزون مباشرة؛ يبقى الطلب بحالة `requested` إلى أن يعتمد المدير الطلب من الـAPI أو Django Admin. عند الاعتماد، يُعاد المخزون ويُحدّث الطلب إلى `returned` عندما تكون كل عناصره المعادة معتمدة.

## فحوصات ما قبل التسليم

نفّذ الفحصين التاليين بعد أي تعديل:

```bash
cd server
.venv/bin/python manage.py test store

cd ../client
npm run build
```

تغطي اختبارات Django التسجيل وتسجيل الدخول والتحقق من الكوبون وإنشاء الطلب وحساب الخصم وتغيير المخزون وسياسة الإرجاع وطلب الإرجاع، إضافة إلى إنشاء المراجعة وحذفها وإنشاء المنتج وتطبيق التخفيض عليه.
